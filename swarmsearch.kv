#:kivy 1.0.9


<ServerBubble>:
    size: 50, 50 
    canvas:
        Ellipse:
            pos: self.pos
            size: self.size      

<SearchGateway>:
    f_query: querybar
    ball: bub
    GridLayout:
        rows: 2
        padding: 10
        spacing: 10
        
        canvas:
            Rectangle:
                pos: self.center_x - 5, 0
                size: 10, self.height
        ServerBubble:
            id: bub
            center: self.parent.center

            
        GridLayout:
            rows: 2
            cols: 2
            TextInput:
                id: querybar
                # Properties for the TextInput widget
            Button:
                # Properties for the button
            DropDown:
                # Properties for the first DropDown widget
            DropDown:
                # Properties for the second DropDown widget
